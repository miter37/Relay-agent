#!/bin/sh
case " $* " in
  *" --version "*|*" -V "*|" version ") echo "codex mock 9.9.9"; exit 0 ;;
  *" --help "*|*" -h "*) echo "exec --sandbox --ask-for-approval --output-last-message --output-schema --ephemeral --search"; exit 0 ;;
esac
export RELAY_PROVIDER_NAME=codex
exec python3 "$(dirname "$0")/mock_ai_cli.py" "$@"
