- [x] 2026-08-12 Relay Home reset: registered Tasks/Projects, Task/Project Run history, Artifacts, lineage, logs, requests, workspaces, results, reviews, schedules, and routines were cleared. Config, Worker definitions, authentication/runtime token, and capability audit history were preserved.
- [x] Project Run Workspace is integrated as the default detail surface: selected Runs use the full detail width, with inline stage summary, shared Artifact preview, Review Panel, and a preserved return path to the catalog.
- [x] Workspace review actions use the existing daemon/API contract; artifact detail/content responses are mirrored into Workspace and legacy Artifacts views; 91 Project Run/Review GUI tests pass.
- [x] GUI catalog selection is consistent: Task/Project/Routine lists now select on current-item change, matching Run behavior for mouse and keyboard navigation.
- [x] Task and Routine editors now remain open during asynchronous saves, preserve fields on daemon rejection, and close only after confirmed success.
- [x] Review comments are scoped per Review, rerun/reject require feedback, duplicate actions are blocked while pending, and successful actions clear the draft.
- [x] Task and Project detail Run-history IDs are clickable and route to the matching Task Run or Project Run workspace.
- [x] Routine editor now offers guided daily/weekly/monthly/every-N-days/once controls, with schedule JSON retained under Advanced editing.
- [x] GUI finished-list polling is section-aware; logs remain selected-detail-only, reducing background refreshes outside active workspaces.
- [x] Task Run and Project Run detail surfaces expose pending Review guidelines, round/budget, and inline Confirm/Rerun/Reject actions.
- [x] Global Quick find with Ctrl+K routes loaded Tasks, Projects, Task Runs, Project Runs, and Reviews from the persistent app shell.
- [x] Unified Artifact Explorer is implemented inline across Task Run, Project Run, and Reviews: shared format-aware preview, metadata/path actions, and review-scoped candidate content reads; 837 tests pass.
- [x] Project Runs v1.1 information architecture is implemented: Pipeline is topology/status-only, Artifacts is the result catalog, Timeline shows attempts and not-started markers, and Inspector is an explicit toggleable evidence drawer.
- [x] Task Run and Project Run catalogs default to the existing date/status tree and provide a real segmented grouping toggle: Task Runs can group by registered Task, Project Runs by Project, with duplicate same-day children labeled `(2)`, `(3)`, and so on.
- [x] Project Runs Artifact Preview replaces the public Steps tab: final outputs are pinned first, Task Artifacts are grouped below with duplicate `Files/result.json` hidden when the Result group already owns it, JSON defaults to a same-size 13px indented report-text view with a Tree view toggle, and Pipeline Artifact double-click navigates to the selected Preview.
- [x] JSON report indentation uses explicit non-breaking spaces in addition to CSS padding so nested rows remain visibly indented in Qt rich text.
- [x] Artifact actions use the generic `Open` label and Qt's cross-platform default-app opener; `Show folder` receives the Artifact's immediate parent directory without applying a second parent traversal.
- [x] Task Run detail browsers are mouse/keyboard selectable for copy; Overview uses a borderless modern key/value layout, wraps Requested task within the viewport, and suppresses horizontal scrolling.
- [x] Task Run polling no longer re-renders identical Run detail/list content; content updates are deferred while a QTextBrowser selection is active, then applied after selection ends to prevent flicker and drag-copy loss.
- [x] Project Run polling now skips identical catalog/detail payloads and buffers changed data while the user is reading Workspace content; an inline update notice lets the user apply it deliberately.
- [x] Project Run Workspace review evidence now exposes Orchestrator evaluation, human handoff reason, and round/task-run history; completed review actions clear Workspace controls and drafts.
- [x] Project Run's duplicate visible Artifacts tab is removed. Pipeline Inspector now offers Open Task (registered definition) and View result (node-focused Artifact/Review in Workspace); Pipeline Artifact activation also routes to Workspace.
- [x] Project Run Pipeline rendering is now batched: initial card/edge assembly suppresses intermediate paints, per-node Artifact responses update only the matching card, identical graph payloads skip rebuilds, and old cards are hidden before deletion so Windows does not briefly promote them to top-level flicker windows.
- [x] Task Run detail now uses the tab order Overview, Progress, Inputs, Artifacts, Logs, Events. Task and Answer are integrated into Overview; its compact summary expands/collapses immediately via See more/See less, even while result text is selected, and Progress renders one readable row per attempt.
- [x] Runs Logs now retain the visible buffer when the 1-second poll reaches EOF with an empty chunk; subsequent bytes append, and changing Attempt/stream/filter starts a fresh buffer.
- [x] Runs Overview headings now share one left alignment: Result and See more sit in the padded toolbar, metadata starts closer below, and the labels are colored with Requested Task capitalization.
- [x] Project Runs no longer exposes the internal steps table as a stray 100x30 widget beside Group by; the catalog also hides its unnecessary horizontal scrollbar.
- [x] New/Edit Project dialog (`relay/gui/projects.py`) fixed: Task selection is a QComboBox picker (was a hand-typed "Name (task_id)" text cell), the dialog scrolls instead of letting its three tables push Save/Cancel off-screen, and Save no longer closes before the POST resolves - a daemon rejection now shows the real `error_message` in the still-open dialog with every row intact.
- [x] `docs/superpowers/plans/2026-08-10-project-orchestrator.md` Tasks 1-9 implemented: schema v15, override overlay, Tier-0 deterministic repair, Tier-1 LLM agent/Supervisor with budget enforcement, narration hooks auto-wired into `ProjectRuntime`, `/v1/project-runs/{id}/orchestrator` + CLI, GUI Orchestrator tab + Project editor settings, and `tests/test_orchestrator_e2e.py` proving a real connection-role mismatch is auto-repaired with zero agent calls and a no-Orchestrator Project is byte-for-byte unchanged. Not reproducible in this sandbox (no installed worker CLI): a schema-mismatch actually repaired by an LLM-authored addendum, and a real Tier-1 call — both covered instead at the mechanism level with a stubbed agent, per the plan's own Task 5 test design. See `wiki/decisions.md` for the authority/budget model.
- [x] Manual real-Qt visual pass completed for populated Project Run review/Workspace states at 1280x720 and 1024x700; the status badge and review surface render correctly. Error/partial, cancelled, approval, retry, and unsupported-worker cases remain to be exercised with live fixtures.
- [ ] Approve/reject UI for `awaiting_approval` Project Runs shipped; edit-with-files (`approve ... /edit`) deferred.
- [ ] Backend `resolve_step_inputs` now tags each resolved entry with `from_node`/`from_role` so receipt `resolved_inputs` can render "A1 <- pick(result)" in the inspector; legacy resolved entries written before this change still need a backfill pass (or the GUI keeps reading `input_manifest_json` as a fallback — current inspector already does this).
- [ ] Review and merge Draft PR #14 (`feat/g5-custom-agent-apps`) after confirming the final G5 scope.
- [ ] Reconcile README's legacy `relay add-agent` description with the current manifest-backed Agent App workflow.
- [ ] Start G6 packaging/platform operations only after G5 is accepted.
- [ ] Add a production embedding backend; current semantic search intentionally falls back to FTS5.
- [ ] Extend lifecycle import/export from Task Runs to full Project/Routine operational-history restoration.
- [ ] Resolve pre-existing repository-wide Ruff format-check violations before release acceptance; focused changed-file checks and full tests pass.
- [ ] Re-run `docs/Relay_GUI_Readability_and_Usability_Hardening_Plan_v1.0.md`'s own screen/popup checklist to confirm nothing beyond its cited contrast and color-literal defects (both fixed by the Design Grammar v1.1 rollout) remains open.
- [ ] Validate and fix GUI user-action flows per `docs/superpowers/plans/2026-08-04-gui-user-scenario-validation.md`; visual grammar changed under the Design Grammar v1.1 rollout so prior validation notes may be stale.
- [ ] `Relay_GUI_Design_Grammar_Modernization_Plan_v1.1.md` §13 item 11 is done for the six sections (1280x720 and 1024x700, DPI 100/125/150, populated data) and all nine dialogs; populated Project Run review/Workspace states were also checked on real Qt. What remains unexercised is error/partial Run states and the approval flow, which need a real failing Run to reproduce.
- [ ] GUI visual checks must run with the real Qt platform, never `QT_QPA_PLATFORM=offscreen`: this sandbox's offscreen backend has no fonts (`QFontDatabase.families()` is empty) so every glyph renders as a tofu box.
- [x] Pipeline legend chips now use semantic QSS states (`needs_review`, `needs_approval`, etc.) instead of passing literal hex colors; a regression test covers the review/approval/running mappings.
- [ ] Full `python -m unittest discover -s tests` still exceeds the local 120–180 second execution windows without reporting a failure. The 327-test impacted/regression suite passes; identify the long-running suite segment before release acceptance.
- [ ] Run a real-platform Qt visual pass for the new Task Interface/Project Wait/Delivery editor states; current GUI verification uses the established offscreen widget suite only.
